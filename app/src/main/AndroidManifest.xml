<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.learning">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="Cooldown Testing V0.1"
        android:roundIcon="@mipmap/cooldownsquare"
        android:supportsRtl="true"
        android:theme="@style/Theme.Learning"
        tools:targetApi="31"
        android:icon="@mipmap/cooldownsquare">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TimerListActivity"/>
        <activity android:name=".NewCooldownActivity"/>

    </application>

</manifest>
